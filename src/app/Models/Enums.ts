// Enum representing different types of content
export enum postType {
    POST, // Represents a post
    COMMENT, // Represents a comment
    REPLY // Represents a reply to a comment
}
// Enum representing different states of liking or disliking content
  export enum Like {
    DISLIKE, // Represents a dislike reaction
    LIKE, // Represents a like reaction
    NONE // Represents no reaction (neither like nor dislike)
}